spring:
  cloud:
    function:
      definition: newOrganisationActionConsume
    stream:
      bindings:
        newOrganisationActionConsume-out-0: 
          destination: organisation-destination
          group: crm-group
